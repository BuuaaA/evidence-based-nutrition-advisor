# 参与维护

当前维护版本为 **V2.0**。欢迎提交能让证据判断更准确、结果更易核查的改进。

## 适合的贡献

- 修正会导致证据层级、适用性或安全性误判的规则；
- 新增可复现的检索、去重、统计或报告工具；
- 添加能暴露真实失败模式的行为用例；
- 改善无障碍、隐私、文件安全或跨平台兼容性；
- 修正文档、安装说明和当前版本的使用示例。

请带上具体问题和可复现的例子。新规则需要说明它防止了什么误判；文案修改也请指出读者原来会在哪里看不懂或误解。

## 开发流程

1. Fork 仓库，按一个问题提交一组改动；
2. 修改 `SKILL.md` 前，看看细则是否更适合放入 `references/`；
3. 修改脚本时，补充能验证实际行为的测试；
4. 不要提交健康档案、凭据、受版权保护的论文全文或生成的 `webr-offline/`；
5. 运行：

```powershell
python -m unittest discover -s tests -p "test_*.py"
```

6. 若修改统计逻辑，运行可用的跨引擎金标准校验，并记录本机 R/Python/包版本；
7. 在 Pull Request 中写清楚触发问题、改动后的行为、验证结果和已知限制。V1.0.1 的案例与 GRADE 字段是历史测试材料；修改它们时请保留版本归属，不要标成 V2.0 的新审计。

## 修改证据方法规则

修改 EAL、QCC 或其他方法规则时，请给出原始手册、指南或方法研究的链接，并说明适用范围。期刊影响因子、研究篇数和机械总分不能替代质量评价。可能改变高风险人群建议的改动，需要同时检查安全边界和反例。

## 报告安全问题

涉及凭据泄露、路径穿越、HTML 注入、隐私暴露或不安全下载的问题，请不要先公开 Issue，按 `SECURITY.md` 的方式私下报告。
